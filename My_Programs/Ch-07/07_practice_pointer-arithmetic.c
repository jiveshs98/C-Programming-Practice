#include <stdio.h>

int main(){

    // // Using integer pointer

    // int i = 34;
    // int *ptr = &i;

    // // Using %u will show a warning, but it will help us to understand the change in pointer values.

    // printf("\nThe value of pointer is: %u\n", ptr);
    // ptr = ptr + 1;  // Will increase the value by 4 bytes
    // printf("\nThe value of pointer after increment is: %u\n", ptr);
    // return 0;



    // Using char pointer

    // char c = 34;
    // char *ptr = &c;
    // printf("The value of pointer is %u\n", ptr);
    // ptr = ptr + 1;  // Will increase the value by 1 byte
    // printf("\nThe value of pointer after increment is: %u\n", ptr);
    // return 0;



    // // Using float pointer

    // float f = 3.4;
    // float *ptr = &f;
    // printf("The value of pointer is %u\n", ptr);
    // ptr = ptr + 1;  // Will increase the value by 4 bytes
    // printf("\nThe value of pointer after increment is: %u\n", ptr);
    // return 0;
}