#include <stdio.h>

int main(){

    // To calculate area of rectangle using hard coded inputs

    int length = 4, breadth = 2;
    printf("\nArea of Rectangle is: %d\n\n", length * breadth);
    return 0;
}